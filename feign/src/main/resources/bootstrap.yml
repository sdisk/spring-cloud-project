spring:
  application:
    name: feign
  zipkin:
    base-url: http://localhost:9411
    sender:
      type: web
  sleuth:
    integration:
      enabled: false
    scheduled:
      skip-pattern: "^org.*HystrixStreamTask$"
    sampler:
      probability: 1